# Phonon-1 experimental CUDA runtime — pinned to the verified configuration.
# Install in two steps (qwen-asr must be installed without its dependencies):
#
#   pip install -r requirements-cuda.txt
#   pip install qwen-asr==0.0.6 --no-deps
#
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.8.0
transformers==4.57.6
safetensors>=0.4.5
soundfile>=0.12.1
numpy
